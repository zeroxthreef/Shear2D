
array(string) compile_static() /* doesnt really matter if were compiling statically or not. Will just return -l:libname.a if static an dpass args to cmake if needed */
{
	array(string) libs;
	
	return libs;
}

bool compile_clean()
{
	
	return false;
}