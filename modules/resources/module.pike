array(string) compile(bool do_static, string cc, string includel, string libl, string extra)
{
	array(string) libs = ({"m"}); /* for lib m (math) */
	
	
	
	return libs;
}

bool clean()
{
	
	return true;
}